<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <% @title = "#{controller.controller_name.humanize} #{controller.action_name.humanize}" rescue ""  %>
  <title>EPICS BDE TOOL - <%=@title%></title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>

  <script language="javascript">
      tstUserKeyboardPref = "<%=preferred_user_keyboard rescue 'qwerty'%>"
      tstUsername = "<%#= current_user.username unless current_user.nil? %>";
      tstCurrentDate = '<%= Time.now.strftime("%Y-%m-%d") %>';
      function openPopUp(url){
          if(window.opener){

              closeOnExit = false;

              window.location = url;

          } else {

              currentpopup = window.open(url,'winname',
                              'directories=no,titlebar=no,toolbar=no,location=no,' +
                              'status=no,menubar=no,scrollbars=yes,resizable=yes,width=600,height=550');

          }
      }
  </script>

  <%= csrf_meta_tag %>
</head>

<body id="container">
<div id="content">
  <div id="description" style="color:#777;position:absolute;margin-top:-70px;">
  </div>
  <%= yield %>
  <div id="footer">
  </div>
</div>
</body>

<%= javascript_include_tag "/touchscreentoolkit/lib/javascripts/touchScreenToolkit.js" %>

</html>
